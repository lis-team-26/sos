(ocamllex
 (modules orchestratorLexer))

(ocamllex
 (modules contractLexer))

(menhir
 (modules orchestratorParser))

(menhir
 (modules contractParser))

(library
 (name contract)
 (modules contract contractAST contractAST_pp contractLexer contractParser)
 (libraries utils))

(library
 (name orchestrator)
 (modules
  orchestrator
  orchestratorAST
  orchestratorAST_pp
  orchestratorLexer
  orchestratorParser
  symbolicInterpreter
  orchestratorUtils)
 (modules_without_implementation orchestratorAST)
 (libraries soteria contract policyChecker utils)
 (preprocess
  (pps soteria.ppx))
 (flags :standard -open Soteria.Logs.Import))

(library
 (name policyChecker)
 (modules policyChecker)
 (libraries contract))

(library
 (name utils)
 (modules utils))
