(library
 (name lib)
 (modules_without_implementation ast)
 (libraries soteria)
 (preprocess
  (pps soteria.ppx))
 (flags :standard -open Soteria.Logs.Import))

(ocamllex
 (modules lexer contractLexer))

(menhir
 (modules parser contractParser))